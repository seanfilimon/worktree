[workspace]
resolver = "2"
members = [
    "crates/worktree-protocol",
    "crates/worktree-store",
    "crates/worktree-ipc",
    "crates/worktree-engine",
    "crates/worktree-server",
    "crates/worktree-bg",
    "crates/worktree-git",
    "crates/worktree-cli",
    "crates/worktree-sdk",
    "crates/worktree-e2e",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "LicenseRef-W0rkTree-Public-License-1.0"

# Single source of truth for dependency versions. Crates inherit with
# `<dep>.workspace = true` and may add crate-specific features on top.
[workspace.dependencies]
# Internal crates
worktree-protocol = { path = "crates/worktree-protocol" }
worktree-store = { path = "crates/worktree-store" }
worktree-ipc = { path = "crates/worktree-ipc" }
worktree-engine = { path = "crates/worktree-engine" }
worktree-git = { path = "crates/worktree-git" }
worktree-sdk = { path = "crates/worktree-sdk" }

# Async runtime
tokio = { version = "1", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
toml = "0.8"

# Wire protocol
prost = "0.13"
prost-types = "0.13"
tonic = "0.12"
tonic-build = "0.12"
protoc-bin-vendored = "3"

# Storage
zstd = "0.13"
fs4 = "0.12"

# Core primitives
blake3 = "1"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"

# Observability
tracing = "0.1"
tracing-subscriber = "0.3"

# IPC
interprocess = { version = "2", features = ["tokio"] }

# Windows process control (daemon spawn without handle inheritance)
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_Threading",
] }

# CLI / filesystem
clap = { version = "4", features = ["derive"] }
colored = "2"
walkdir = "2"
notify = "7"
dirs = "5"
tempfile = "3"
escargot = "0.5"

# Git interop
git2 = "0.19"

[workspace.lints.rust]
unsafe_code = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
